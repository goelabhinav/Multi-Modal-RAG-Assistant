[project]
name = "multi-modal-rag-assistant"
version = "0.1.0"
description = "Multi-Modal RAG Assistant for QA and incident investigation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
